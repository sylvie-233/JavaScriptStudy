# jsonwebtoken

## 基础介绍

jwt


## 核心内容
```yaml
jsonwebtoken:
    Secret:
    SignOptions:
    sign(): # 签发jwt（生成token）
        payload:
        secretOrPrivateKey:
        options:
            expiresIn:
    verify(): # 验证jwt
```