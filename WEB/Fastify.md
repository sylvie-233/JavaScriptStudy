# Fastify

## 基础介绍

javascriptr 高性能web框架

nestjs底层框架


## 核心内容
```yaml
fastify:
    FastifyInstance:
        logger:
        --- # fastify实例
        get():
        listen():
            port:
        post():
        register(): # 注册插件
        route():
            url:
            method:
            handler:
            schema: # 参数校验
                body:
                params:
                querystring:
                response: # 响应参数
                    propertry:
                    required:
                    type:
```